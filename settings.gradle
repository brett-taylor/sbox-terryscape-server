rootProject.name = 'terryscape'

include 'engine'
include 'engine-internal'
// include 'content'
include 'cache'

//  project(":content").projectDir.eachDir { dir ->
//      if (!dir.name.startsWith('src')) {
//          include "content:${dir.name}"
//      }
//  }
